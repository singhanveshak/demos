which commands?
	whoami		ls		cd(meaning of   .   /   .. ) 	cat		mv		touch		mkdir		cp		rm		wc		head 	tail
	clear	apropos		--help		top		apt,snap(introduce as linux playstore, show them, then ask to install something like VLC player)	
	ctrl+C	piping		output redirection>		curl 	wget		nano (introduce as Notepad for linux)
